/* exported setValue */

// use bracket notatoin to set the value of an object's property and add to an object
// return the object

function setValue(object, key, value) {
  object[key] = value;
  return object;
}
