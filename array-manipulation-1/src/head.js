/* exported head */
// the first element of an array is retrieved using index zero ([0])
// take the array literal found within the parameter and apply index zero
// return the result from the function

function head(array) {
  return array[0];
}
