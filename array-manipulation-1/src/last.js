/* exported last */

// the last element of an array is retrieved using index array.length minus 1 ([array.length - 1])
// take the array literal found within the parameter and apply
// return the result from the function

function last(array) {
  return array[array.length - 1];
}
