/* exported getFirstChar */
// check the first character of the string using indexes (0 is a representatino of the first character)
// take the value of the string in the parameter along with [0]
// return the result from the function

function getFirstChar(string) {
  return string[0];
}
